# AI Replacing Jobs Handoff

Updated: 2026-05-23

Project path: `C:\Users\xuner\Documents\工具站\sites\aireplacingjobs`

## Current Status

AI Replacing Jobs v1 is implemented and deployed as a deterministic public toolsite.

Local URL:

```text
http://localhost:3000
```

Production target:

```text
https://aireplacingjobs.org
```

Primary live URL:

```text
https://www.aireplacingjobs.org
```

Repository:

```text
https://github.com/HK-COOL/aireplacingjobs
```

## Implemented

- Independent ShipAny Template Two project workspace.
- English-only public v1 site.
- Rebranded metadata, navigation, legal pages, sitemap, robots, logo, favicon, touch icon, browser icon, and preview image.
- Deterministic AI Job Risk Checker with input validation, reset, example data, and copy result.
- Homepage, checker page, five SEO resource pages, and ten occupation pages.
- Quality verifier for sitemap, route registration, blocked surfaces, brand assets, and core analytics events.
- Launch readiness verifier for Vercel config, domain hygiene, sitemap exclusions, API removal, brand assets, and secret leakage checks.
- V1 proxy blocking for demo, private, auth, admin, settings, and API routes.
- Removed template `src/app/api` routes for v1.
- Visual QA fixes for resource tables and mobile checker text clipping.
- Vercel Analytics auto-load on Vercel with core tool events: `tool_check_risk`, `tool_apply_preset`, and `tool_copy_result`.
- Reusable tracked CTA links with `longtail_cta_click` for homepage, SEO pages, and occupation pages.
- IndexNow key file and submission script for the 17 public sitemap URLs.

## Verification

Passed:

- `pnpm exec tsx scripts/verify-ai-job-risk.ts`
- `pnpm exec tsx scripts/verify-site-quality.ts`
- `pnpm exec tsx scripts/verify-launch-readiness.ts`
- `pnpm exec tsc --noEmit`
- `VERCEL=1 CI=1 NEXT_TELEMETRY_DISABLED=1 pnpm exec next build --webpack`
- `pnpm exec tsx scripts/submit-indexnow.ts`

## Known Caveats

- Unused template page files remain under admin/auth/chat/settings areas, but they are blocked in `src/proxy.ts` and excluded from sitemap/navigation.
- The build logs a local SQLite config warning because template services try to read DB-backed config. V1 does not use DB features.
- Browser first-screen QA was checked with headless screenshots for desktop and mobile. The screenshots were used for QA and then removed from the repo.
- GitHub and Vercel are connected. The first production commit was pushed to `main`.
- Cloudflare DNS is configured for the apex and `www`; Vercel reports both custom domains verified. Local DNS caches may briefly lag for the apex.
- IndexNow accepted the 17 public sitemap URLs on 2026-05-23.

## Suggested Next Work

1. Verify the latest production deploy after this analytics update.
2. Submit `https://aireplacingjobs.org/sitemap.xml` to Google Search Console and Bing Webmaster.
3. Re-check Vercel Analytics for `longtail_cta_click`, `tool_check_risk`, and `tool_copy_result` after a few manual interactions.
4. Start Stage 2 with one small growth sprint, not a broad content dump.
