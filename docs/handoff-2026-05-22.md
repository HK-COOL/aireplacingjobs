# AI Replacing Jobs Handoff

Updated: 2026-05-22

Project path: `C:\Users\xuner\Documents\工具站\sites\aireplacingjobs`

## Current Status

AI Replacing Jobs v1 is implemented locally as a deterministic public toolsite.

Current local URL:

```text
http://localhost:3000
```

Production target:

```text
https://aireplacingjobs.org
```

## Implemented

- Independent ShipAny Template Two project workspace.
- English-only public v1 site.
- Rebranded metadata, navigation, legal pages, sitemap, robots, logo, favicon, touch icon, browser icon, and preview image.
- Deterministic AI Job Risk Checker with input validation, reset, example data, and copy result.
- Homepage, checker page, five SEO resource pages, and ten occupation pages.
- Quality verifier for sitemap, route registration, blocked surfaces, and brand assets.
- Launch readiness verifier for Vercel config, domain hygiene, sitemap exclusions, API removal, brand assets, and secret leakage checks.
- V1 proxy blocking for demo, private, auth, admin, settings, and API routes.
- Removed template `src/app/api` routes for v1.
- Visual QA fixes for resource tables and mobile checker text clipping.

## Verification

Passed:

- `pnpm exec tsx scripts/verify-ai-job-risk.ts`
- `pnpm exec tsx scripts/verify-site-quality.ts`
- `pnpm exec tsx scripts/verify-launch-readiness.ts`
- `pnpm exec tsc --noEmit`
- `VERCEL=1 CI=1 NEXT_TELEMETRY_DISABLED=1 pnpm exec next build --webpack`

## Known Caveats

- Unused template page files remain under admin/auth/chat/settings areas, but they are blocked in `src/proxy.ts` and excluded from sitemap/navigation.
- The build logs a local SQLite config warning because template services try to read DB-backed config. V1 does not use DB features.
- Browser first-screen QA was checked with Edge headless screenshots for desktop and mobile. The screenshots were used for QA and then removed from the repo.
- `git` is not available in the current local shell, so repository status and commits were not created from this environment.
- `aireplacingjobs.org` returned `NXDOMAIN` during domain preflight, so production cutover needs domain registration or DNS configuration first.

## Suggested Next Work

1. Push the project to the production GitHub repository.
2. Connect Vercel preview deployment.
3. Verify the preview URL in desktop and mobile browser.
4. Cut over `aireplacingjobs.org`.
5. Submit sitemap to IndexNow, Google Search Console, and Bing Webmaster.
6. Add analytics events before expanding occupation pages or starting monetization tests.
